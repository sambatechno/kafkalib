syntax = "proto3";

package kafkalib.opts;

import "google/protobuf/descriptor.proto";

extend google.protobuf.MessageOptions {
  optional string topic_name = 60000; // Use a high, unique tag number
}
